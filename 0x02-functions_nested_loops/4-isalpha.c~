#include "holberton.h"

/**
 * function that checks for alphabetic character
 */
int _isalpha(int c)
{
if (c >= 97 && c <= 122)
{
_putchar(1);
}
else if (c >= 65 && c <= 90)
{
_putchar(1);
}
else
{
_putchar(0);
}
}
